#!/bin/bash

# Overwrite the user configs for the Waybar menu bar with the bArchTi defaults and restart the service.

barchti-refresh-config waybar/config.jsonc
barchti-refresh-config waybar/style.css
barchti-restart-waybar
